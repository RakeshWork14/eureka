apiVersion: apps/v1
kind: Deployment
metadata:
  name: i27-eureka-stage
spec:
  repliacas: 1
  selector:
    matchLabels:
      name: i27-eureka-stage
  template:
    metadata:
      labels: i27-eureka-stage
  spec:
    containers:
    - name: i27-eureka-stage
      image: DIT

---
apiVersion: v1 
kind: Service
metadata:
  name: i27-eureka-stage-svc
spec:
  selector:
    app: i27-eureka-stage #this name matches with the above matadata->labelname, otherwise it won't create the endpoint
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8761
  type: NodePort            